openapi: 3.1.0
info: { title: Orders, version: 1.0.0 }
paths: {}
components:
  schemas:
    OrderResponse:
      type: object
      properties:
        id: { type: string }
        currency: { type: string }
