# ChangeBench baselines

Store reviewed `ChangeBenchReport` JSON files here when a real analyzer executor
is available. Baseline comparison ignores duration measurements but treats every
semantic case result, metric, diagnostic, digest, and segment as significant.

Phase 2 defines and tests the format. Analyzer-specific baselines begin in the
analyzer implementation phases so no synthetic output is mistaken for evidence.
